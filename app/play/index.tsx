import React from 'react';

import { GetStaticProps } from 'next';

const Play: React.FC = () => {
    return (
        <div>
            <h1>Play</h1>
            <p>Select a game to play!</p>
        </div>
    );
};

export default Play;
